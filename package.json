{
  "scripts": {
    "test:unit": "jest --config=jest.config.unit.js",
    "test:integration": "jest --config=jest.config.integration.js",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "26.0.1"
  }
}
